{
  "name": "Chatts",
  "version": "1.0.0",
  "main": "app.js",
  "license": "ISC",
  "scripts": {
    "start": "dotenv -v NODE_ENV=prod node app",
    "dev": "dotenv -v NODE_ENV=dev nodemon app",
    "test": "jest",
    "build": "babel ./public -d ./dist --copy-files --delete-dir-on-start",
    "build:w": "babel ./public -d ./dist -w --copy-files --delete-dir-on-start"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dompurify": "^2.4.1",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "express-minify": "^1.0.0",
    "helmet": "^6.0.0",
    "jsdom": "^20.0.2",
    "socket.io": "^4.5.3"
  },
  "devDependencies": {
    "@babel/core": "^7.20.2",
    "@babel/preset-env": "^7.20.2",
    "dotenv": "^16.0.3",
    "dotenv-cli": "^6.0.0",
    "jest": "^29.3.1",
    "nodemon": "^2.0.20",
    "socket.io-client": "^4.5.3"
  }
}
